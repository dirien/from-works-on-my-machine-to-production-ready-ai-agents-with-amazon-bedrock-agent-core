[project]
name = "basic-bedrock-deployment-scripts"
version = "0.1.0"
description = "Test scripts for Bedrock fraud detection agent"
requires-python = ">=3.10"
dependencies = [
    "boto3>=1.34.0",
]
